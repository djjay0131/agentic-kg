"""Cypher query helpers and contracts shared across the codebase.

Modules here are intentionally small and string-returning (or thin repo
wrappers) so the verify-gate audit can grep for non-conforming queries.
"""
